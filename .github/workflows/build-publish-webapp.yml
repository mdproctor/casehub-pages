name: Build and Publish Webapp

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      skip_tests:
        description: 'Skip tests during build'
        required: false
        type: boolean
        default: false
      create_latest_tag:
        description: 'Update latest tag (only for main branch)'
        required: false
        type: boolean
        default: true

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-full-webapp:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'yarn'

      - name: Enable Corepack
        run: corepack enable

      - name: Install JavaScript dependencies
        run: yarn install --immutable

      - name: Build all components and webapp
        run: yarn workspaces foreach -At run build

      - name: Create distribution archives
        run: |
          cd webapp/dist
          zip -r ../pages-webapp.zip .
          cd ..
          sha256sum pages-webapp.zip > pages-webapp.zip.sha256

      - name: Upload webapp artifact
        uses: actions/upload-artifact@v4
        with:
          name: pages-webapp
          path: |
            webapp/pages-webapp.zip
            webapp/pages-webapp.zip.sha256
          retention-days: 30

      - name: Upload to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            webapp/pages-webapp.zip
            webapp/pages-webapp.zip.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}