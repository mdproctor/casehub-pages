pages:
- name: Reports Detail
  rows:
  - columns:
    - components:
      - html: >-
          <div style="padding: 4px 0 12px; color: #6b7280; font-size: 12px">
            ↳ Loaded from <code>reports.dash.yml</code>
          </div>
  - columns:
    - span: 4
      components:
      - type: title
        properties:
          text: Reports Generated
          size: h3
      - type: metric
        properties:
          lookup:
            uuid: report-stats
            filter:
            - column: Metric
              function: EQUALS_TO
              args:
              - Generated
            group:
            - functions:
              - source: Count
                function: SUM
          columns:
          - id: Count
            pattern: '#,##0'
    - span: 4
      components:
      - type: title
        properties:
          text: Avg Generation Time
          size: h3
      - type: metric
        properties:
          lookup:
            uuid: report-stats
            filter:
            - column: Metric
              function: EQUALS_TO
              args:
              - AvgTime
            group:
            - functions:
              - source: Count
                function: SUM
          columns:
          - id: Count
            pattern: '#,##0 ms'
    - span: 4
      components:
      - type: title
        properties:
          text: Failed Reports
          size: h3
      - type: metric
        properties:
          lookup:
            uuid: report-stats
            filter:
            - column: Metric
              function: EQUALS_TO
              args:
              - Failed
            group:
            - functions:
              - source: Count
                function: SUM
          columns:
          - id: Count
            pattern: '#,##0'
  - columns:
    - components:
      - type: title
        properties:
          text: Recent Report Runs
          size: h3
      - type: data-table
        properties:
          pageSize: 6
          sortable: true
          lookup:
            uuid: report-runs
